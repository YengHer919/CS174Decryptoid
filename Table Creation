Note that average username length is 6-30 char
Note that salt is 8 len, but can have a max of 16 char

CREATE TABLE credentials (
    name VARCHAR(36) NOT NULL,
    id INT(9) NOT NULL,
    email VARCHAR(32) NOT NULL,
    password CHAR(32) NOT NULL,
    salt1 VARCHAR(16),
    salt2 VARCHAR(16)
);

Note that the longest cipher (char) is 'Double Transposition' and is 20 characters
CREATE TABLE cipher_logs (
    time TIMESTAMP(6), 
    input VARCHAR(500), 
    cipher VARCHAR(20), 
    key VARCHAR(32)
);
